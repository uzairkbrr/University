#include <stdio.h>
#include <string.h>

int main() {
 
 int size  ;
	
	printf("Enter Size for Array "); 
	scanf("%d", &size);

	char name[size] ;
	char name2[size];
 
	strcpy(name2 , name) ;
 
 	printf("Name After Updating "); puts(b);
 
    return 0;
}
